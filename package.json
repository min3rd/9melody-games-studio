{
  "name": "9melody-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run prisma:migrate && npm --prefix web run prisma:seed && npm --prefix web run dev",
    "dev:docker": "docker compose -f docker-compose.dev.yml up --build",
    "dev:docker:down": "docker compose -f docker-compose.dev.yml down",
    "lint": "npm --prefix web run lint",
    "test": "npm --prefix web run test"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  }
}
